[package]
name = "freecad-kernel"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "GeometryKernel trait: the single geometry contract of the FreeCAD Rust stack"

[dependencies]
freecad-core = { workspace = true }
