[package]
name = "freecad-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Renderer-neutral data types for the FreeCAD Rust stack (mesh, ids, primitives)"

[dependencies]
